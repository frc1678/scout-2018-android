<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:title="Start Timer"
        android:id="@+id/beginTimerButton"
        app:showAsAction="ifRoom"
        android:checkable="true"/>
    <item
        android:id="@+id/currentScout"
        android:title="Current Scout"
        android:orderInCategory="101"
        app:showAsAction="always"/>
    <item
        android:id="@+id/mainOverride"
        android:title="Override Schedule"
        app:showAsAction = "always"/>
    <item
        android:id="@+id/setScoutIDButton"
        android:title="Set Scout ID"
        app:showAsAction="ifRoom"/>
</menu>